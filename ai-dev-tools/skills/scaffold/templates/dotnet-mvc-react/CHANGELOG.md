# dotnet-mvc-react — Template CHANGELOG

## v0.0.0

Templates pending; stack reserved. Selecting this stack via
`/scaffold --bootstrap --stack dotnet-mvc-react` exits with the message
"stack registered, templates pending" and writes no files.
