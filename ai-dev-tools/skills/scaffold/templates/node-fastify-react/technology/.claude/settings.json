{
  "_comment": "Technology layer additions — deep-merged into root .claude/settings.json by /scaffold Step B3.5. Only contributes extra allowedHosts. The typecheck hook is handled by the {{TYPECHECK_HOOK_COMMAND}} placeholder in root/.claude/settings.json (see references/placeholder-resolution.md).",
  "sandbox": {
    "network": {
      "allowedHosts": [
        "stackoverflow.com",
        "developer.mozilla.org",
        "nodejs.org",
        "vitejs.dev",
        "react.dev",
        "fastify.dev",
        "orm.drizzle.team",
        "turbo.build",
        "pnpm.io",
        "tailwindcss.com"
      ]
    }
  }
}
