# node-fastify-react — Template CHANGELOG

## v1.0.0

Initial versioned release. Documents the pre-existing state of the stack as
first shipped.

### Added (baseline)
- Root layer: `CLAUDE.md`, `.claude/settings.json`, `.claude/hotspots.md`,
  `.claude/hookify.block-git-operations.local.md`,
  `.claude/hookify.block-sql-writes.local.md`.
- Technology layer: `CLAUDE.md` (appended into root `CLAUDE.md`),
  `.claude/settings.json` (deep-merged into root `.claude/settings.json`),
  `.claude/hookify.warn-db-push.local.md`.
- Package layer: `packages/<name>/CLAUDE.md`,
  `packages/<name>/.claude/settings.json`.
- Placeholder set documented in
  `ai-dev-tools/skills/scaffold/references/placeholder-resolution.md`.
